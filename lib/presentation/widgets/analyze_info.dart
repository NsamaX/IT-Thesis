import 'package:flutter/cupertino.dart';

class AnalyzeInfoWidget extends StatelessWidget {
  const AnalyzeInfoWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
